//! Cadenza core: domain entities, value objects, policies, ports, application services.
//!
//! Populated in M1. This crate must never depend on `cadenza-infra` or `cadenza-ui`.

#![forbid(unsafe_code)]
