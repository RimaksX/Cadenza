import { Theme } from "../theme/tokens.slint";
import { ScrollFade } from "../components/ScrollFade.slint";

// One track and how often it was played through.
export struct TopTrackData {
    rank: string,
    title: string,
    subtitle: string,
    plays: string,
}

// A number worth looking at, and what it counts.
component Figure inherits Rectangle {
    in property <string> value;
    in property <string> label;

    // Wide enough for the longest thing it holds — "SKIPPED" under "2h 05m" —
    // with a floor under it, because four of these at 176 and their gaps want
    // 728 and the smallest window offers 648. A stated width cannot shrink, so
    // the fourth figure hung off the edge — and, worse, the column they sit in
    // took their width, so every row of the standings below hung off it too
    // (`MASTER_ISSUES` 131).
    preferred-width: Theme.px(176);
    min-width: Theme.px(140);
    height: Theme.px(78);
    // The one radius. These were the last square corners in the interface, on
    // a card standing beside eight rounded ones everywhere else.
    border-radius: Theme.radius;
    border-width: 1px;
    border-color: Theme.line-soft;
    background: Theme.bg-1;

    VerticalLayout {
        x: Theme.space-4;
        y: round((parent.height - Theme.px(54)) / 1px / 2) * 1px;
        width: parent.width - Theme.space-4 * 2;
        height: Theme.px(54);
        spacing: Theme.px(2);

        // Mono, and this is not a preference. The serif's figures are
        // old-style: 6 and 8 rise, 3 and 5 drop, 1 and 2 sit at x-height. In a
        // name that is a grace and in a column of statistics it is a fence
        // falling over — measured on the rendered page, the 6 of "26" stood
        // seven pixels above its own 2 (`MASTER_ISSUES` 131). Mono's figures
        // are lining and tabular, which is what this face is here for: the
        // design says numbers are its job, and this page predates the rule.
        Text {
            text: root.value;
            font-family: Theme.mono;
            font-size: Theme.text-title;
            height: Theme.px(34);
            vertical-alignment: center;
            color: Theme.ink-0;
            overflow: elide;
        }

        Text {
            text: root.label;
            font-family: Theme.mono;
            font-size: Theme.text-label;
            height: Theme.px(18);
            vertical-alignment: center;
            letter-spacing: Theme.label-spacing;
            color: Theme.ink-mute;
            overflow: elide;
        }
    }
}

// One line of the standings.
component TopRow inherits Rectangle {
    in property <TopTrackData> data;

    // The shape of a library row, because it is one: a track's name, who made
    // it, and a number about it. It was shorter and set in the sans, which made
    // the same object look like two different kinds of thing on two pages.
    height: Theme.px(52);

    HorizontalLayout {
        y: round((parent.height - Theme.px(26)) / 1px / 2) * 1px;
        height: Theme.px(26);
        spacing: Theme.space-4;

        VerticalLayout {
            width: Theme.px(34);
            alignment: center;

            Text {
                text: root.data.rank;
                font-family: Theme.mono;
                font-size: Theme.text-label;
                height: Theme.px(26);
                horizontal-alignment: right;
                vertical-alignment: center;
                letter-spacing: Theme.number-spacing;
                color: Theme.ink-mute;
            }
        }

        VerticalLayout {
            horizontal-stretch: 1;
            alignment: center;

            Text {
                text: root.data.title;
                font-family: Theme.serif;
                font-size: Theme.text-lead;
                height: Theme.px(26);
                vertical-alignment: center;
                color: Theme.ink-1;
                overflow: elide;
            }
        }

        VerticalLayout {
            preferred-width: Theme.px(240);
            min-width: Theme.px(120);
            alignment: center;

            Text {
                text: root.data.subtitle;
                font-family: Theme.sans;
                font-size: Theme.text-body;
                height: Theme.px(26);
                vertical-alignment: center;
                color: Theme.ink-2;
                overflow: elide;
            }
        }

        VerticalLayout {
            width: Theme.px(92);
            alignment: center;

            Text {
                text: root.data.plays;
                font-family: Theme.mono;
                font-size: Theme.text-label;
                height: Theme.px(26);
                horizontal-alignment: right;
                vertical-alignment: center;
                letter-spacing: Theme.number-spacing;
                color: Theme.ink-2;
            }
        }
    }

    Rectangle {
        y: parent.height - 1px;
        height: 1px;
        background: Theme.line-soft;
    }
}

// A month of listening, and nothing older than that.
export component ListeningView inherits Rectangle {
    in property <[TopTrackData]> top;
    in property <string> summary;
    in property <string> heard;
    in property <string> listens;
    in property <string> tracks;
    in property <string> skip-rate;
    /// False when this listener keeps no history at all.
    in property <bool> keeping;

    background: Theme.bg-0;

    VerticalLayout {

        VerticalLayout {
            vertical-stretch: 0;
            padding-left: Theme.page-inset;
            padding-right: Theme.page-inset;
            padding-top: Theme.page-top;
            padding-bottom: Theme.page-bottom;
            spacing: Theme.space-2;

            Text {
                text: "THE LAST THIRTY DAYS";
                font-family: Theme.mono;
                font-size: Theme.text-label;
                height: Theme.px(18);
                vertical-alignment: center;
                letter-spacing: Theme.label-spacing;
                color: Theme.ink-furniture;
            }

            Text {
                text: "Listening";
                font-family: Theme.serif;
                font-size: Theme.text-display;
                height: Theme.heading-box;
                vertical-alignment: bottom;
                color: Theme.ink-0;
            }

            Text {
                text: root.summary;
                font-family: Theme.mono;
                font-size: Theme.text-label;
                height: Theme.px(18);
                vertical-alignment: center;
                letter-spacing: Theme.number-spacing;
                color: Theme.ink-2;
            }
        }

        // One child of the page, not two. A pair of conditionals side by side in
        // a layout are two children whichever one is showing, and the layout
        // sizes both — which left the visible one with half a page and squeezed
        // four figures into a corner.
        VerticalLayout {
            vertical-stretch: 1;
            padding-left: Theme.page-inset;
            padding-right: Theme.page-inset;
            padding-bottom: Theme.space-5;

        // Nothing is kept, so there is nothing to draw and a reason to say why.
        if !root.keeping: VerticalLayout {
            vertical-stretch: 1;
            alignment: start;
            spacing: Theme.space-2;

            Text {
                text: "Nothing is being kept";
                font-family: Theme.serif;
                font-size: Theme.text-title;
                height: Theme.px(34);
                vertical-alignment: center;
                color: Theme.ink-2;
            }

            Text {
                text: "History is off for this profile, so nothing you play is written down. Turn it on in Settings and this page fills up as you listen.";
                font-family: Theme.sans;
                font-size: Theme.text-body;
                height: Theme.px(44);
                wrap: word-wrap;
                color: Theme.ink-mute;
            }
        }

        if root.keeping: VerticalLayout {
            vertical-stretch: 1;
            spacing: Theme.space-5;

            HorizontalLayout {
                vertical-stretch: 0;
                alignment: start;
                spacing: Theme.space-2;

                Figure {
                    value: root.heard;
                    label: "HEARD";
                }
                Figure {
                    value: root.listens;
                    label: "LISTENS";
                }
                Figure {
                    value: root.tracks;
                    label: "TRACKS";
                }
                Figure {
                    value: root.skip-rate;
                    label: "SKIPPED";
                }
            }

            VerticalLayout {
                vertical-stretch: 1;
                spacing: Theme.space-2;

                HorizontalLayout {
                    height: Theme.px(26);
                    spacing: Theme.space-2;

                    VerticalLayout {
                        alignment: center;

                        Text {
                            text: "PLAYED THROUGH MOST";
                            font-family: Theme.mono;
                            font-size: Theme.text-label;
                            height: Theme.px(18);
                            vertical-alignment: center;
                            letter-spacing: Theme.label-spacing;
                            color: Theme.ink-furniture;
                        }
                    }

                }

                // The standings are a list beside the figures rather than the
                // page itself, so they fade where they continue — the same
                // sentence the moods and the presets say.
                Rectangle {
                    vertical-stretch: 1;

                    standings := Flickable {
                        width: parent.width;
                        height: parent.height;
                        viewport-height: rows.preferred-height;

                        rows := VerticalLayout {
                            width: parent.width;
                            alignment: start;

                            for track in root.top: TopRow {
                                data: track;
                            }
                        }
                    }

                    ScrollFade {
                        y: parent.height - self.height;
                        width: parent.width;
                        more: standings.viewport-height + standings.viewport-y > standings.height;
                    }
                }
            }
        }
        }
    }
}
