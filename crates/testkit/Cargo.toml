[package]
name = "cadenza-testkit"
description = "Test fixtures, fakes and helpers shared across Cadenza crates."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[dependencies]
cadenza-core.workspace = true

# temp_db needs the real migration runner: a helper that built the schema its own
# way would let the tests pass against a schema production never sees.
cadenza-infra.workspace = true
