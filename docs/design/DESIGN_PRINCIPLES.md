# Design principles

Placeholder. The real content is supplied by the project owner as a markdown document
and consumed in M15; see [../DESIGN_INTEGRATION.md](../DESIGN_INTEGRATION.md).

Until then the UI uses a neutral placeholder theme. Nothing in `crates/core` may depend
on anything written here.
